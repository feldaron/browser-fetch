{
  "name": "browser-fetch",
  "version": "2.0.0",
  "private": true,
  "description": "Chrome-based retailer listing discovery and price verification for LaptopValue.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/*.mjs && node --check scripts/*.mjs && node --check tests/*.mjs",
    "test": "node --test tests/*.test.mjs",
    "browser-fetch": "node src/cli.mjs",
    "browser-fetch:issue": "node src/issue-request.mjs"
  },
  "dependencies": {
    "playwright": "1.55.0"
  }
}
